apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ template "laravel.name" . }}-{{ .Release.Time.Seconds }}-env
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "laravel.name" . }}
    chart: {{ template "laravel.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-10"
type: Opaque
data:
  .env: |-
{{ .Files.Get .Values.app.envFilePath | b64enc | indent 4 }}