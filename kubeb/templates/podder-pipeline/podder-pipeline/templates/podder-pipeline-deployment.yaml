apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{ template "podder-pipeline.fullname" . }}-airflow
  labels:
    app: {{ template "podder-pipeline.name" . }}
    chart: {{ template "podder-pipeline.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "podder-pipeline.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "podder-pipeline.name" . }}
        release: {{ .Release.Name }}
    spec:
      volumes:
        - name: {{ .Release.Name }}-{{ template "podder-pipeline.name" . }}-env
          secret:
            secretName: {{ .Release.Name }}-{{ template "podder-pipeline.name" . }}-{{ .Release.Time.Seconds }}-env
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.app.repository }}:{{ .Values.app.tag }}"
          imagePullPolicy: {{ .Values.app.pullPolicy }}
          command: airflow
          env:
            {{- range $task := .Values.tasks }}
            - key: {{ $task.name }}
              value: "{{ $task.name }}-service:50051"
            {{- end }}
          volumeMounts:
            - name: {{ .Release.Name }}-{{ template "podder-pipeline.name" . }}-env
              mountPath: /usr/local/poc_base/envs/.env
              subPath: .env
          ports:
            - name: grpc
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}

---
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ template "podder-pipeline.name" . }}-{{ .Release.Time.Seconds }}-env
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ template "podder-pipeline.name" . }}
    chart: {{ template "podder-pipeline.chart" . }}
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  annotations:
    "helm.sh/hook": pre-install
    "helm.sh/hook-weight": "-10"
type: Opaque
data:
  .env: |-
{{ .Files.Get .Values.app.envFilePath | b64enc | indent 4 }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "podder-pipeline.fullname" . }}-service
  labels:
    app: {{ template "podder-pipeline.name" . }}
    chart: {{ template "podder-pipeline.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ template "podder-pipeline.name" . }}
    release: {{ .Release.Name }}
